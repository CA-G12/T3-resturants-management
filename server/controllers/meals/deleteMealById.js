const { deleteById } = require('../../database/queries/index');

// const deleteMeal = (req, res) => {
// get id to be done later
//   console.log('hello');
//   deleteById('meals', id).then((data) => console.log(data.rows)).catch((erorr) => console.log(erorr));
// };

// module.exports = deleteMeal;
